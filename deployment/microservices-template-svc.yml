apiVersion: v1
kind: Service
metadata:
    labels:
        app: microservices-template
    name: microservices-template
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: microservices-template
    
